<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.TopacAdemy_android" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="key">
        <item name="android:backgroundTint">@color/darkGreen</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/textSizeOfKey</item>
    </style>

    <style name="rightCol">
        <item name="android:backgroundTint">@color/darkOr</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/sizeOfRightCol</item>
    </style>

    <style name="prognostication">
        <item name="android:background">@color/darkGray</item>
    </style>

    <style name="ThemeOverlay.Topacademy_android.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_900</item>
        <item name="fullscreenTextColor">@color/light_blue_A400</item>
    </style>
</resources>